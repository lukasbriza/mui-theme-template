import type { Shape } from '@mui/system'

export const shape: Shape = {
  borderRadiusNone: 0,
  borderRadiusXs: 2,
  borderRadiusS: 4,
  // `borderRadius` has to be here. It's a matter of preference if we use `borderRadius` or `borderRadiusM`.
  borderRadius: 8,
  borderRadiusM: 8,
  borderRadiusL: 12,
  borderRadiusXl: 16,
  borderRadius2Xl: 20,
  borderRadius3Xl: 24,
  borderRadius4Xl: 32,
}
